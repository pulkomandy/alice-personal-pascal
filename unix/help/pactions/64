|4,72
     This action is used on an empty Block Statement placeholder.  It
allows you to type in a keyword (e.g. FOR) and change the Block Statement
into the construct corresponding to the keyword (e.g. a FOR loop).
