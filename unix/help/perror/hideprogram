|6,73
Can't hide the whole program
    You have highlighted the entire program, then attempted to hide it.
    You can only HIDE sections of a program, not the whole thing.  Most of
    the time, this only happens if you pushed the HIDE key by accident; if
    so, just ignore this message.
